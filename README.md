# 🌍 Country Clustering with KMeans

This project demonstrates **KMeans clustering** to group countries based on their **geographic coordinates** (longitude & latitude) and visualize the clusters on a map.

---

## 📊 Dataset

The dataset `Countries-exercise.csv` must include:  

- **Country** – Name of the country  
- **Longitude** – Longitude coordinate  
- **Latitude** – Latitude coordinate  

> Make sure the CSV file is in the same folder as this script.

---

## ⚙️ Requirements

Python libraries needed:  

- `pandas`  
- `matplotlib`  
- `seaborn`  
- `scikit-learn`  

Install them with:  

```bash
pip install pandas matplotlib seaborn scikit-learn

